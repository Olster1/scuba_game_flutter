name: scuba_game
description: "A new casual suba diving game."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.36.0
  xml: ^6.6.1
  forge2d: ^0.14.2+1
  flame_forge2d: ^0.19.2+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
